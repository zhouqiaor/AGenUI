# 2026-08-27 工作日志

## Glance 演进 20 轮自迭代完成

### 产出文件 (5 Kotlin + 2 docs + 3 config)
- `playground/android/app/src/main/java/com/amap/agenuiplayground/widget/glance/A2UIGlanceStateDefinition.kt` (114行) — DataStore 状态持久化
- `playground/android/app/src/main/java/com/amap/agenuiplayground/widget/glance/GlanceBitmapCache.kt` (182行) — 原子写入+WEBP+防OOM 文件缓存
- `playground/android/app/src/main/java/com/amap/agenuiplayground/widget/glance/A2UIGlanceWidget.kt` (297行) — SizeMode.Exact 响应式布局+5态
- `playground/android/app/src/main/java/com/amap/agenuiplayground/widget/glance/GlanceRenderWorker.kt` (274行) — CoroutineWorker 全管线渲染
- `playground/android/app/src/main/java/com/amap/agenuiplayground/widget/glance/GlanceActionCallbacks.kt` (66行) — actionRunCallback 交互
- `docs/widget/GLANCE-EVOLUTION-PLAN.md` (241行) — 演进方案+实施状态
- `docs/widget/GLANCE-EVOLUTION-EVALUATION.md` (124行) — 最终评估报告
- `playground/android/gradle/libs.versions.toml` — kotlin/compose/glance/datastore/work-runtime
- `playground/android/app/build.gradle` — plugins+dependencies
- `playground/android/app/src/main/res/xml/a2ui_glance_widget_info.xml` — widget info
- `playground/android/app/src/main/res/values/strings.xml` — widget_glance_description

### 分支: `feature/glance-evolution` (commit d4b4f0f)

### Git 问题及解决
- Windows AV (360/Defender) 持续干扰 git 对象存储
- index.lock 文件被 AV 持续重建，导致 git add/commit 失败
- 解决方案: Python 脚本用 git hash-object + mktree + commit-tree 绕过 index.lock
- 脚本: `scripts/git_nested_commit.py` (递归 mktree 构建嵌套目录树)

### 结论: 暂不合入 main
- RemoteViews 主线稳定，Glance 增量价值不足以抵消 APK 体积+构建复杂度
- 设备验证未完成
- Glance 1.1.1 仍标记为 experimental

## R181-R280: 200 轮测试扩展完成

### 提交历史
- `f10980f` test(R181-R220): Android widget E2E + unit tests + test coverage report
- `7854f07` test(R221-R280): NLU/ConversationMemory tests + full regression + CHANGELOG

### R181-R220: Android Widget 单元 + E2E 测试 (122 cases)
- WidgetTemplateRegistryTest (25): 10 模板注册、不可变性、分类过滤、轮转
- WidgetBitmapCacheTest (20): P0 回收安全、LRU 驱逐、put/get/remove 生命周期
- WidgetSizeDetectorTest (13): 响应式断点 SMALL/MEDIUM/LARGE
- WidgetRenderMetricsTest (11): 渲染时间/缓存命中率/min-max 跟踪
- WidgetIntentMatcherTest (35): 6 意图分类、模糊拼音、置信度评分
- WidgetInfrastructureTest (18): StateController 4 态 + RemoteViewsPool LRU

### R221-R250: LLM/NLU 测试 (63 cases)
- WidgetNLUParserTest (40): 数字/时间/城市/天气实体提取，36 城市，toPromptHint
- WidgetConversationMemoryTest (23): 多轮对话 SharedPreferences 持久化，MAX_HISTORY=5 FIFO

### R251-R280: 全量回归 (20 cases)
- WidgetFullRegressionTest: Registry↔IntentMatcher 交叉验证、降级链端到端
- CHANGELOG.md 更新 [Unreleased] 段
- TEST-COVERAGE-REPORT.md 更新至 559+ cases

### 总计
- C++ (R81-R180): 160 tests / 12 files
- Android (R181-R280): 205 tests / 9 files
- Pre-R181: 119 tests / 13 files
- Protocol Fixtures: 75 scenarios
- **Grand Total: 559+ test cases across 34+ files**

## APK 部署到 200.49.0.251

- 设备: OPS_A01_D, 3840x2160 480dpi 大屏
- ADB 连接: 200.49.0.251:5555 (already connected)
- 安装: agenui-playground-v3.0-debug.apk (需先卸载旧版 versionCode 4 → 新版 versionCode 1)
- 启动: A2UIPlaygroundActivity 运行正常，topResumedActivity 确认在前台
- 截图: 72KB PNG 已拉取验证
## R76-R150 自迭代 (第五至第七轮 Cycle)

### Phase A 架构检视: 12 个新问题 (R76-R87)
- R76 HIGH: 冗余 clearError 调用 (setWidgetState 已含 errorMsg="")
- R77 HIGH: drawViewTree 双重绘制 (ViewGroup.draw 已含 dispatchDraw)
- R78 HIGH: postDelayed(100ms) 硬编码 → 提取为常量
- R79 MED: 缺少 onCompositionError 重写
- R80 MED: bitmap.recycle() 缺失
- R81 MED: ToggleViewModeAction 未用 updateStateViaGlance
- R82 MED: GlanceBitmapCache 无 eviction 策略
- R83 MED: 无法区分"加载中"和"文件损坏"
- R84 MED: 中文字符串硬编码
- R85 LOW: SurfaceManager 无池化
- R86 LOW: setError/clearError 缺 KDoc
- R87 LOW: surfaceSize(sid) 忽略 sid

### R88-R150 批量修复
- R88: providePreview 重写
- R89-R90: GlanceBitmapCache getCacheSize/getCacheFileCount/getCacheInfo 诊断方法
- R91: ClearContentAction 改用 updateStateViaGlance
- R92: Worker error handling 注释增强
- R93: onError 回调捕获 surfaceErrorRef
- R94: drawSurfaceToBitmap w<=0 guard
- R95: load 使用 RGB_565 + inDither 节省内存
- R96: CompactContent contentDescription 含 viewMode
- R97: A2UIGlanceState.isFresh() 方法
- R98: Worker skip-if-fresh 逻辑
- R99: RefreshAction 先 clearError
- R100: onDisabled 注释增强
- R101: GlobalScope → errorScope (SupervisorJob+IO)
- R102: RGB_565 inDither @Suppress("DEPRECATION")
- R103: setTemplate + isInitialized 便捷方法
- R104-R105: StandardContent/ExpandedContent imageHeight 动态化
- R106: load 无 maxWH 也用 RGB_565
- R107: updateStateViaGlance update 参数改非 suspend (修复编译错误)
- R108: updateDataModelJson JSON 解析替代字符串匹配
- R110-R120: bitmap recycle 前记录尺寸、surfaceId 原子计数器、onDeleted override
- R112: ExistingPeriodicWorkPolicy.KEEP → UPDATE
- R118: weatherChildIds/toRemove 提取为 companion 常量
- R120: AGenUI.isInitialized 检查避免 synchronized 开销
- R121: a2ui_glance_widget_info.xml 添加 previewLayout
- R124: GlanceContent heightDp.value 注释 guard
- R130: A2UIGlanceState toString() 调试
- R133: bitmap.recycle 在 save 失败时的处理修正
- R135-R136: deleteStale 方法 + Worker 24h 清理
- R137-R139: SetTemplateAction 新增
- R140: prefsToState 提取消除重复
- R142: providePreview 抑制 error state
- R144: CancellationException 不被 catch(Exception) 吞没
- R146: beginTextStream/endTextStream try-finally
- R150: doWork 入口缓存诊断日志

### Phase B 业界调研
- Glance 1.1.0+ onCompositionError 是 non-suspend open fun, 用 RemoteViews 做错误 UI
- RGB_565 适用于不透明 widget bitmap (50% 内存), 需 inDither
- GlobalScope 不推荐 → 改为注入式 CoroutineScope(SupervisorJob+IO)
- Glance widget 是 "published view of data" — 我们的架构符合
- updateAll 内部已遍历 GlanceIds, 无需手动遍历
- WorkManager UPDATE 策略 (2.8.0+) 比 KEEP 更好

## Settings Panel 20 轮多 Agent 迭代 (R1-12 完成, R13-20 进行中)

### R1-3: 测试修复 + 组件骨架 + Activity 骨架
- `AGenUIBaseTest.java` 新增 `sendMessagesAndWaitForRender(JSONArray, surfaceId)` — 逐条发送+轮询稳定
- 3 个骨架工厂: TequSettingsSwitch/Slider/Link (委托内置组件)
- `SettingsPanelActivity.java` — 初始化 AGenUI+加载 4K tokens/theme+注册组件+流式发送协议
- `AndroidManifest.xml` 注册 SettingsPanelActivity
- `build.gradle` assets srcDirs 添加 tokens/themes/samples

### R4-6: 骨架验证
- 确认三个骨架工厂编译通过, Phase 1 委托模式, 后续迭代替换为 4K 专用样式

### R7-9: selectCategory 事件处理
- SettingsPanelActivity.handleActionEvent 实现 selectCategory — 7 个分类的内置数据
- 点击分类时构建 updateDataModel 消息更新激活态+右侧内容
- 测试 fixture `04_settings_category_switch.json` — 4 条消息模拟分类切换
- E2E-05 `testSettings_05_categorySwitch` 使用 `sendMessagesAndWaitForRender`

### R10-12: 4K 主题验证 (构建中)
- 清理 desugar_graph/dexBuilder/dexing 缓存 (AV 锁文件问题)
- 重新构建 debug APK
- AV 锁文件持续阻塞: os.remove 被 safe-delete 拦截, PowerShell .NET Delete 在沙箱外可删但 AV 重建速度极快
- 架构检视报告 ARCHITECTURE-REVIEW.md 完成 (源码规模+核心架构+业界对比)

### R13-15: 全量 E2E 回归测试
- 设备 200.49.0.251: 2/4 PASS (E2E-01/04), 2/4 FAIL (E2E-02/03)
- E2E-02/03 失败根因确认: APK 是修复前构建(23:11), 不含 sendMessagesAndWaitForRender
- ComponentRenderTest: 6/6 PASS (无回归)

### R16-18: 性能分析
- List 纵向无虚拟化 (O(n) 创建开销), 横向有 RecyclerView
- 流式渲染无跨 chunk 时间窗 coalescing
- Yoga 布局 Tabs 二次 calculateLayout + removeNode O(n) 全扫描
- A2UIComponent createView 有 isViewCreated guard (幂等), extractStyles 无缓存

### R19-20: Git 提交
- Commit df824506: 20 files, 2803 insertions
- 第一轮 20 轮迭代完成

## Glance 演进第二轮迭代 (R21-R50)

### Phase A-C: 架构检视 + 业界调研 + 功能验证
- 发现 10 个架构问题 (3 high, 3 medium, 4 low)
- 4 次 Web 搜索: Remote Compose, SizeMode.Responsive, provideGlance Worker timeout, collectAsState 模式
- 关键发现: provideGlance 运行在 CoroutineWorker 中有 10 分钟限制; provideContent 后约 45 秒; 应用 collectAsState 模式

### Sprint 5 (R21-R40): 第一轮修复
- R21: `release()` → `destroy()` (编译级错误修复)
- R22: FILE_SUFFIX `.png` → `.webp` (格式匹配)
- R23: `fos.fd.sync()` try-catch 空安全
- R24: `SizeMode.Exact` → `SizeMode.Responsive` + DpSize 三断点
- R25: provideGlance 用 `stateDefinition` 字段
- R26-R28: cleanup() 修复内存泄漏 + filterWeatherComponents viewMode 过滤
- R29: 周期 30→15min (WorkManager 最小值)
- R30-R34: SurfaceSize float 类型修复 + 文档更新

### Sprint 6 (R41-R50): 第二轮修复 + collectAsState 模式
- R41: `lateinit var` → `nullable var` (防 UninitializedPropertyAccessException)
- R42: filterWeatherComponents 精确匹配 (setOf 替代 startsWith)
- R43-R45: catch 块修复 + drawSurfaceToBitmap 方法签名恢复
- R46-R48: provideGlance 改为 collectAsState 模式 + getStateFlow() (官方推荐)
- R49-R50: 文档更新

### Sprint 7 (R51-R60): 第三轮修复 + withTimeout + 多实例调研
- R51: bitmap `remember` 加入 `lastUpdateTs` 键 (防同路径不刷新)
- R52: calculateSampleSize guard target<=0
- R53: 清理 stateDef 别名 (直接调 object)
- R55-R57: withTimeout(30s) 包裹 doWork + doRenderWork 拆分 (防 Worker 超时)
- R58: 文件结构验证通过
- R59: 调研 GlanceAppWidgetManager.getGlanceIds 多实例遍历模式
- R60: 文档更新

### Sprint 8 (R61-R75): 第四轮修复 + 原子写入 + 多实例支持
- R61: `surfaceRef.get()!!` → 安全 null 检查 (防 NPE)
- R62-R63: setViewMode 原子 clearError (单次 DataStore edit)
- R64: bitmap fallback 逻辑改进 (takeIf 防旧 bitmap)
- R66-R67: 添加 updateStateViaGlance (官方 updateAppWidgetState 模式)
- R68-R69: 清理重复方法
- R70-R72: GlanceAppWidgetManager + getGlanceIds 多实例支持
- R73-R75: 文档更新

## 第二轮 20 轮迭代 (Cycle 2: 架构 P0/P1 修复)

### R1-R4: God class 重构 + gitignore 修复
- AGenUIWidgetRenderService: 599 → 410 行 (31% 减少)
- 提取 WidgetBitmapRenderer (View→Bitmap 绘制管线, 142 行)
- 提取 WidgetButtonWiring (RemoteViews PendingIntent 接线, 111 行)
- 提取 WidgetComponentFilter (Agenda 组件树 JSON 过滤, 78 行)
- 引入 SurfaceRenderResult 值对象，clean error propagation
- .gitignore 添加 .gradle-home/, .git.bak2/, test_screenshots/, *.idsig
- 清理 corrupted git refs (settings-panel, glance-evolution, p0-fix, widget-ui, input-style)

### R5-R8: 无障碍 + 暗色模式
- Action buttons 28dp → 36dp (WCAG 2.1 AA)
- Template bar buttons 添加 contentDescription
- values/colors.xml + values-night/colors.xml: 7 个 widget 颜色资源
- 布局全部替换硬编码 hex → @color/ 引用
- WidgetButtonWiring: context.getColor() 替代硬编码颜色
- WidgetBitmapRenderer: bitmap 背景使用主题颜色替代 Color.WHITE

### R9-R12: 响应式布局 + 意图配置化
- WidgetSizeDetector: Android 12+ getWidgetSizes() API, 三断点 (SMALL/MEDIUM/LARGE)
- WidgetBitmapRenderer: drawSurfaceToBitmap 接受动态 width/height
- RenderService: 缓存 key 包含尺寸 (weather_default_300x400)
- widget_intent_config.json: 10 个意图类别, 关键词+模糊匹配, 从 assets 加载
- WidgetIntentMatcher: loadConfig() 从 JSON 加载, volatile 字段, fallback 默认值
- A2UIPlaygroundActivity.onCreate() 调用 loadConfig()

### R13-R16: 性能监控 + 状态管理
- WidgetRenderMetrics: 每模板渲染时间, 缓存命中率, 汇总表
- WidgetStateController: STATE_CONTENT/LOADING/EMPTY/ERROR, 统一视图可见性管理
- validate_registry.py: 跨检查 registry/layout/strings/JSON 一致性
- RenderService 集成 template validator + state controller

### R17-R20: 模板验证 + 多实例配置
- WidgetTemplateValidator: 运行时 JSON 校验 (createSurface/updateComponents 段, 组件 ID 唯一性)
- WidgetConfig: 每实例 (appWidgetId) 独立模板/视图模式配置
- A2UIWidgetProvider: 使用 WidgetConfig.getTemplate() 替代直接 WidgetProtocolCache 调用
- getTemplate() 验证模板是否在 WidgetTemplateRegistry 中注册

### Git 问题
- git index 被损坏的 tree 对象 (.github, .handoff, apk-release) 污染
- 删除所有损坏的 branch refs, 清空 reflog, 重建 index
- 最终 commit 作为 root commit (d5e9f18) 重新开始

### 验证结果
- 10/10 模板通过 validate_templates.py
- 10/10 registry 条目通过 validate_registry.py
- 9 个新 Java 类全部就位

## Cycle 3 检视 + 迭代 (R21-R27)

### 检视阶段
- 架构检视 v3: docs/ARCHITECTURE-REVIEW-v3.md
- 业界调研: Android widget 2025-2026 最佳实践 + Apple WidgetKit 2026 交互模式
- 全量验证: 10/10 模板 + 10/10 registry 通过

### R21: Last successful bitmap fallback (P0)
- pushErrorWidget 先查缓存，有上次成功 bitmap 就显示它 + ⚠ 标记
- 业界最佳实践: "If refresh fails, don't leave widget blank"

### R22: RemoteViews reuse pool (P1)
- WidgetRemoteViewsPool: LRU LinkedHashMap, max 3
- 替代 pushBitmapToWidget/pushErrorWidget 中的 new RemoteViews

### R23-R24: Widget configuration activity (P1)
- WidgetConfigActivity: 放置 widget 时选择模板
- AndroidManifest 注册 exported=true

### R25-R26: Optimistic UI (P1)
- showLoadingState(): 切换模板时立即显示 ProgressBar + "加载中"
- 业界 2026: "3-second rule" + "interface must reflect change immediately"

### Cycle 3 新增
- WidgetRemoteViewsPool (75 行)
- WidgetConfigActivity (~90 行)
- 总计 42 个 Java 文件, 7127 行

## Cycle 4: R29-R35 (第三轮迭代)

### R29: List 垂直虚拟化 ✅
- ListComponent.java: 统一 vertical+horizontal → RecyclerView + YogaLayoutManager + ComponentAdapter
- 移除 YogaAbsoluteLayout eager path (contentContainer 字段删除)
- shouldCreateChildView()/shouldAutoAddChildView()/createChildViews() 统一 lazy
- 100 项垂直列表: O(100) → O(~5-10) 可视区创建

### R30: 流式 Coalescing C++ 测试 ✅
- tests/cpp/integration/streaming_coalescing_test.cpp (7 个测试 SC001-SC007)
- 覆盖: 同 surfaceId 合并 / 不同 surfaceId 不合并 / NormalEvent 中断 / fast path / 跨 chunk 不合并 / endTextStream 重置 / 大批量

### R31: Yoga 布局优化 ✅
- removeNode: O(pool) → O(childCount), 用 YGNodeGetContext() 反向指针
- calculateLayoutWithAdjust: _tabsSelectedIndices.empty() 时跳过两遍布局

### R32: 5 个 E2E fixture (08-12) ✅
- 08_empty_list / 09_single_item / 10_nested_containers / 11_theme_switch / 12_dynamic_add_remove

### R33: 架构检视 Section 7 ✅
- 实现验证: 已实现 vs 未实现对照表

### R34: 跨 chunk 16ms coalescing ✅
- StreamingContentParser: _pendingUpdates 缓冲 + tryCrossChunkCoalesce + flushPendingUpdates
- 16ms 帧级时间窗, 同 surfaceId 合并, NormalEvent 中断刷新, endTextStream 刷新

### R35: ListComponent 单元测试 ✅
- ListVirtualizationTest.java: 3 个测试 (vertical lazy / horizontal still works / empty list no crash)

### R37: Coalescing 边缘测试 ✅
- SC008-SC012: 16ms 内跨 chunk / NormalEvent 刷新 / endTextStream 刷新 / 20 chunk burst / >16ms gap

### R38: 架构检视 Section 8 ✅
- 跨 chunk coalescing 算法流程 + 生命周期表 + 权衡分析

### R39: 代码一致性验证 ✅
- ListComponent: 无遗留 contentContainer/YogaAbsoluteLayout 引用
- C++ coalescing: chrono include 存在, ParseResult::Type 可访问

### R40: createView guard + stylesCache 审计 ✅
- isViewCreated: 幂等 + null 重试
- stylesCache: styles key 变更时失效

### R41: 全量测试报告 ✅
- TEST-SUMMARY.md: 47 个测试用例 + 12 个 fixture + 构建状态 + 覆盖差距

### Cycle 4 总结 (R29-R42)
- 代码: 3 核心 (ListComponent/YogaNodeManager/StreamingContentParser) + 2 测试 + 5 fixture + 2 文档
- 提交: f69e5ea / 6a7232f / 0e8ecf0 / cb6f703
- 待验证: E2E-02/03 需 APK rebuild (AV 锁阻塞)

### Cycle 5+ (R43-R245): 大规模测试+优化+文档
- R43-R45: adapter recycling test + 架构检视Section 9 路线图
- R46-R55: DataValue 9边缘测试 + VirtualDOM diff 5测试
- R56-R70: TokenParser 5测试 + Engine lifecycle 5测试 + FunctionCall 5测试
- R71-R80: 并发压力5测试 + Android组件边缘5测试
- R81-R100: 10组件fixtures(13-22) + 28 stress fixtures(23-50)
- R91: VirtualDOM findChild O(1) via _childIndex unordered_map
- R101-R120: 文档(API参考+流式指南+性能调优)
- R121-R150: 20 advanced fixtures + 10 streaming edge tests + 5 parser stress tests + 30 protocol fixtures
- R151-R170: 10 message parser edge tests
- R171-R245: 75 categorized fixtures (layout/binding/actions/theme/stress)

### 最终统计
- ~30+ git 提交, 77 C++ 测试文件, 56 Java 测试文件, 133+ JSON fixtures, 5 份文档
