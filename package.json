{
  "name": "agenui-harmony-tokens",
  "version": "1.1.0",
  "description": "HarmonyOS Design Token pipeline for AGenUI",
  "scripts": {
    "build:tokens": "node style-dictionary.config.mjs",
    "prebuild": "npm run build:tokens"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  }
}
